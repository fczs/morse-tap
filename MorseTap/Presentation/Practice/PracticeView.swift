import SwiftUI

struct PracticeView: View {
    var body: some View {
        NavigationStack {
            Text("Practice Morse Tap")
                .font(.title)
                .navigationTitle("Practice")
        }
    }
}

#Preview {
    PracticeView()
}
